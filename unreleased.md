## Unreleased

### Added

- warning output when no changelog entry exists for the requested version
- new input `fail-on-missing-changelog` (default `false`) to fail non-dry-run executions without changelog entries

### Updated

- default unreleased template now resets to heading-only (`## Unreleased`)
